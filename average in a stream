class Solution{
public:	
	vector<float> streamAvg(int arr[], int n) {
	    // code here
	    vector<float>m;
	    float a=0;
	    float sol;
	    for(int i=0;i<n;i++)
	    {
	        a=a+arr[i];
	        sol=a/(i+1);
	        m.push_back(sol);
	    }
	    return m;
	}
};
