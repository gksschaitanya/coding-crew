2. Find the median
class Solution
{
public:
	public:
		int find_median(vector<int> v)
		{
		   int n=v.size();
		   int sol;
		   sort(v.begin(),v.end());
		   if(n%2==0)
		   {
		       sol=int((v[n/2]+v[(n/2)-1])/2);
		       
		   }
		   else
		   sol=v[(n-1)/2];
		 return sol;
		}
};
